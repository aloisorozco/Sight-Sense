from classes.obstacle import Obstacle
from classes.alert import Alert

URGENT_OBSTACLES = {"car", "bicycle", "bus", "train", "truck"}

def process_objects_to_alerts(obstacles : list[Obstacle]) -> Alert:
  pass